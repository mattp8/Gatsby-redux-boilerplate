import colors from './Colors';
import {
    type,
    size,
    style
} from './Fonts';

const theme = {

    //colors
    colors,

    //fonts
    fonts: {
        type,
        size,
        style
    },
};

export default theme